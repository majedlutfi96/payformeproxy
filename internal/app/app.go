package app

// App should represent the composed application with config, logger, proxy server, payment processor, and chain registry.
// New should wire dependencies together.
// Run should start the proxy and optional TUI.
// Shutdown should gracefully stop running components.
